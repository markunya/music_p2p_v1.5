# UniHeunGuidanceStepper — uni Heun with CFG; inherits GuidanceStepperHeun defaults.
#
# guidance_scale: CFG scale (>1 enables null branch).
# cfg_t_start: lower time bound for CFG (inclusive).
# cfg_t_end: upper time bound for CFG (inclusive).
_target_: src.steppers.guidance.UniHeunGuidanceStepper
guidance_scale: 7.0
cfg_t_start: 0.0
cfg_t_end: 1.0
